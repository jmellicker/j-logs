"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const endpoint_with_defaults_1 = require("./endpoint-with-defaults");
const merge_1 = require("./merge");
const parse_1 = require("./parse");
function withDefaults(oldDefaults, newDefaults) {
    const DEFAULTS = merge_1.merge(oldDefaults, newDefaults);
    const endpoint = endpoint_with_defaults_1.endpointWithDefaults.bind(null, DEFAULTS);
    return Object.assign(endpoint, {
        DEFAULTS,
        defaults: withDefaults.bind(null, DEFAULTS),
        merge: merge_1.merge.bind(null, DEFAULTS),
        parse: parse_1.parse
    });
}
exports.withDefaults = withDefaults;
